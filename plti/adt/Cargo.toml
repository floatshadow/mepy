[package]
name = "plti-adt"
version = "0.1.0"
edition = "2024"
description = "Persistent and search-oriented data structures"
repository = "https://github.com/floatshadow/mepy"
exclude = ["content"]

[lib]
path = "src/lib.rs"

[dependencies]

[dev-dependencies]
proptest = "1.11.0"

[lints.rust]
missing_docs = "warn"
unsafe_code = "forbid"

[lints.rustdoc]
broken_intra_doc_links = "deny"
