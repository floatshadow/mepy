method triple(x: Int) returns (r: Int)
   requires x % 2 == 0
   ensures r == 3 * x
{ 
    r := x / 2
    r := 6 * r
}

method client() {
  var z: Int
  // Assertion x % 2 == 0 might not hold.
  // z := triple(7)
  z := triple(6)
  assert z == 18
}
